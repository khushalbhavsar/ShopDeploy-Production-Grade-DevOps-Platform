# ArgoCD Application - Frontend Staging
# This manifest defines the ArgoCD application for the frontend service in staging environment
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: shopdeploy-frontend-staging
  namespace: argocd
  labels:
    app: shopdeploy
    component: frontend
    environment: staging
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: shopdeploy

  source:
    repoURL: https://github.com/khushalbhavsar/ShopDeploy-Cloud-Native-DevOps-Platform.git
    targetRevision: main
    path: helm/frontend
    helm:
      valueFiles:
        - ../../gitops/staging/frontend-values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: shopdeploy-staging

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  revisionHistoryLimit: 10
