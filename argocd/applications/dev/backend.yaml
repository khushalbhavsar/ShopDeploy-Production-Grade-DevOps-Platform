# ArgoCD Application - Backend Development
# This manifest defines the ArgoCD application for the backend service in dev environment
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: shopdeploy-backend-dev
  namespace: argocd
  labels:
    app: shopdeploy
    component: backend
    environment: dev
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: shopdeploy

  source:
    repoURL: https://github.com/khushalbhavsar/ShopDeploy-Cloud-Native-DevOps-Platform.git
    targetRevision: main
    path: helm/backend
    helm:
      valueFiles:
        - ../../gitops/dev/backend-values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: shopdeploy-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  revisionHistoryLimit: 10
